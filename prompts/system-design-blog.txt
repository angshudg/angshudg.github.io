------------------------------------------------
------------------------------------------------
JSON--------------------------------------------
------------------------------------------------
------------------------------------------------
I have uploaded an image of a mind map. Your task is to extract the entire hierarchical structure from this image and convert it into a single, deeply nested JSON object.

**Task Requirements:**
1. **Extraction:** Accurately capture the text from every node and maintain the exact parent-child relationships shown in the image.
2. **Contextual Descriptions:** For every node, you must generate a concise description (~60 words). This description must explain the concept *specifically within the context of the root node's overarching topic*. Do not use your internal knowledge. Search the web for highly specific up-to-date facts to formulate these descriptions. 
3. **Pagination:** If the mind map is too large and the resulting JSON will exceed your single-response output limits, do not truncate the JSON abruptly. Instead, pause at a logical high-level branch, close the JSON block cleanly, and ask me to type 'proceed' to generate the next batch.

**Strict JSON Structure:**
You must format the output using the following recursive structure. Every node (from the root down to the deepest leaf) must be an object with these exact keys:

{
  "node_name": "[The exact text of the node as seen in the image]",
  "description": "[Your generated contextual description for this node]",
  "children": [
    {
      "node_name": "[Child Node 1]",
      "description": "[Contextual description]",
      "children": [ ... ] 
    },
    {
      "node_name": "[Child Node 2]",
      "description": "[Contextual description]"
      // If a node has no children, pass an empty array [].
    }
  ]
}

Please begin extraction and output the JSON block.
Required Output: Downloadable JSON File

------------------------------------------------
------------------------------------------------
blog--------------------------------------------
------------------------------------------------
------------------------------------------------
this json file covers concepts of networking protocols in a structured form. 
you need to write a medium style blog based on this content. 
this post is intended for data science folks who are just starting out on system design concepts, so it should cover the fundamentals and also go to depths required for modern data science skills required in the industry. 
if there are illustrations that you feel should be added for explaining the concepts better, either use ascii art or write a detailed description of the illustration that will be used later as a prompt to generate or search a fitting image. 
feel free to add definitions or descriptions to any topic as you see fit, the json structure is merely a skeleton on top of which you will write the blog.
output an .md file
let me know if you have any questions before you begin

------------------------------------------------
------------------------------------------------
blog rewritten----------------------------------
------------------------------------------------
------------------------------------------------
You are an expert technical writer, systems educator, and frontend developer.

You are given a structured JSON knowledge graph about the topic: "Networking Architecture".

Your task is to transform this JSON into a polished, publication-quality long-form technical article written in the style of a high-quality Medium engineering blog.

Generate TWO files:

1. `content.md`
2. `index.html`

Both files must contain the same article structure, section order, formatting hierarchy, and overall readability.

--------------------------------------------------
TARGET AUDIENCE
--------------------------------------------------

- Data scientists
- ML engineers
- Analytics engineers
- Engineers new to system design and distributed systems

The article should teach concepts from first principles while gradually introducing deeper architectural and industry-level insights.

--------------------------------------------------
WRITING STYLE
--------------------------------------------------

- Medium-style technical writing
- Clear, engaging, and practical
- Avoid textbook-style explanations
- Build intuition before complexity
- Explain WHY concepts exist, not just WHAT they are
- Include tradeoffs, real-world examples, and practical engineering context
- Add smooth transitions between sections

--------------------------------------------------
CONTENT RULES
--------------------------------------------------

- Use the JSON hierarchy as the structural backbone
- Expand beyond the JSON wherever useful
- Add examples, analogies, comparisons, caveats, and modern industry relevance
- Add practical insights for data/ML engineering workflows when relevant
- Include code snippets, pseudocode, APIs, configs, or architecture examples where helpful

--------------------------------------------------
VISUALS
--------------------------------------------------

Whenever a concept benefits from visualization:

In `content.md`:
Insert detailed illustration prompts using this exact format:

[ILLUSTRATION_PROMPT_START]
Detailed image/diagram prompt here
[ILLUSTRATION_PROMPT_END]

Prompts should describe:
- layout
- components
- arrows/data flow
- labels
- visual style
- educational objective

--------------------------------------------------
HTML FILE RULES
--------------------------------------------------

Generate a matching `index.html` version of the article.

Requirements:
- Preserve the same structure and formatting as the Markdown version
- Use semantic HTML5
- Responsive and readable
- Medium-style typography and spacing
- Preserve code block and ASCII formatting
- No frameworks required

IMPORTANT:

In `index.html`:
- Replace every illustration prompt block with an image reference
- Images will later exist inside an `images/` directory beside `index.html`

Use this format in html file for images:
<img src="images/example-diagram.png" alt="Description">

------------------------------------------------
------------------------------------------------
blog split:md-----------------------------------
------------------------------------------------
------------------------------------------------
You are an expert technical writer and systems educator.

You are given a structured JSON knowledge graph.

Your task is to transform this JSON into a polished, publication-quality long-form technical article written in the style of a high-quality Medium engineering blog.

Generate ONLY:

`content.md`

---

## TARGET AUDIENCE

* Data scientists
* ML engineers
* Analytics engineers
* Engineers new to system design and distributed systems

The article should teach concepts from first principles while gradually introducing deeper architectural and industry-level insights.

---

## WRITING STYLE

* Medium-style technical writing
* Clear, engaging, and practical
* Avoid textbook-style explanations
* Build intuition before complexity
* Explain WHY concepts exist, not just WHAT they are
* Include tradeoffs, real-world examples, and practical engineering context
* Add smooth transitions between sections

---

## CONTENT RULES

* Use the JSON hierarchy as the structural backbone
* Expand beyond the JSON wherever useful
* Add examples, analogies, comparisons, caveats, and modern industry relevance
* Add practical insights for data/ML engineering workflows when relevant
* Include code snippets, pseudocode, APIs, configs, or architecture examples where helpful
* Produce a complete article ready for publication

---

## VISUALS

Whenever a concept benefits from visualization, insert detailed illustration prompts using EXACTLY the following format:

[ILLUSTRATION_PROMPT_START]
Detailed image/diagram prompt here
[ILLUSTRATION_PROMPT_END]

Each illustration prompt should clearly specify:

* Layout
* Components
* Arrows and data flow
* Labels
* Visual style
* Educational objective

These prompts will later be converted into diagrams and images.

---

## OUTPUT FORMAT

Return ONLY the markdown file `content.md`.

Do not generate HTML.

Do not generate explanations about your process.

Output valid Markdown file only.


------------------------------------------------
------------------------------------------------
blog split:html---------------------------------
------------------------------------------------
------------------------------------------------
You are an expert frontend developer, technical publisher, and HTML document designer.

You are given a Markdown file named:

`content.md`

Convert it into a publication-quality HTML article.

Generate ONLY:

`index.html`

---

## GOAL

Create an HTML version that preserves:

* Article structure
* Section ordering
* Heading hierarchy
* Lists
* Tables
* Code blocks
* Quotes
* Overall readability

The HTML should visually resemble a polished Medium-style engineering article.

---

## HTML REQUIREMENTS

* Use semantic HTML5
* Responsive layout
* Clean typography
* Comfortable reading width
* Proper spacing and hierarchy
* No external frameworks
* Self-contained CSS inside the HTML file
* Preserve code formatting and ASCII diagrams
* Mobile-friendly

---

## IMAGE CONVERSION RULES

The Markdown file contains blocks in the following format:

[ILLUSTRATION_PROMPT_START]
...
[ILLUSTRATION_PROMPT_END]

These blocks should NOT appear in the final HTML.

Instead:

1. Replace each illustration block with an image reference.
2. Images will exist inside:

`images/`

beside `index.html`.

Use:

<img src="images/example-diagram-N.png" alt="Meaningful description">

Where:

* N is the sequential diagram number (1, 2, 3, ...)
* The alt text should be derived from the illustration prompt content.

---

## OUTPUT FORMAT

Return ONLY the HTML file `index.html`.

Do not regenerate article content.

Do not rewrite sections.

Use the provided Markdown as the single source of truth.

Use the sample.html file uploaded to ground the style and overall flow of index.html

Output a valid HTML file only.
